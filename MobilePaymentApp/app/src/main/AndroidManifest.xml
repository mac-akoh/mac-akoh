<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mobilepaymentapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.NFC" />

    <!-- Features -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.nfc" android:required="false" />
    <uses-feature android:name="android.hardware.nfc.hce" android:required="false" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobilePaymentApp">

        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.MobilePaymentApp.NoActionBar"> {/* Optional: NoActionBar for splash */}
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity"
            android:exported="false"> {/* MainActivity is no longer the launcher */}
            {/* Remove launcher intent-filter if it was here, or ensure it's not here */}
            {/*
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
            */}
        </activity>
        <activity android:name=".auth.LoginActivity" />
        <activity android:name=".auth.RegisterActivity" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".auth.LoginActivity" />
        <activity android:name=".auth.RegisterActivity" />
        <activity android:name=".auth.ForgotPasswordActivity" />
        <activity android:name=".ui.profile.ProfileActivity"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".ui.transactions.TransactionHistoryActivity"
            android:label="Transaction History"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".ui.qrcode.DisplayQrActivity"
            android:label="Your QR Code"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".ui.merchant.MerchantRegistrationActivity"
            android:label="Merchant Registration"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".ui.support.SupportActivity"
            android:label="Help &amp; Support"
            android:parentActivityName=".MainActivity"/>


        <!-- Firebase Messaging Service -->
        <!--
        <service
            android:name=".services.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        -->

        <!-- Placeholder for HostApduService for HCE NFC Payments -->
        <!-- ... (HCE service placeholder) ... -->

        {/* ZXing CaptureActivity declaration (if using zxing-android-embedded) */}
        {/* ... (ZXing declarations remain here) ... */}

    </application>

</manifest>
